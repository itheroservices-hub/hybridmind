// Test file for HybridMind Agentic Workflow
function calculateTotal(items) {
  let total;
  for (let item of items) {
    total += item.price;
  }
  return total;
}

// Try these workflows:
// 1. Select the function and run "⚡ Quick Fix"
// 2. Run "⚡ Add Feature" and say "Add input validation"
// 3. Run "⚡ Run AI Workflow" and say "Fix the bug"
